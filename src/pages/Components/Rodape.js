import styled from "styled-components";

const Footer = styled.footer`
    height: 306px;
`

const Rodape = () => {
    return (
        <Footer>
        
        </Footer>
    );
}

export default Rodape;