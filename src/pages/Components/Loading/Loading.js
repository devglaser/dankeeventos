import loading from "@/pages/Components/Loading/loading.module.css"

const Loading = () => {
    return (
        <div id={loading.loading}>
            <span></span>
            <span></span>
            <span></span>
        </div>
    );
}

export default Loading;